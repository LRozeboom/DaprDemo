# Environment-variable secret store: lets discord.yaml read DISCORD_WEBHOOK_URL from the
# environment instead of committing a real webhook URL to the repo.
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: envvar-secrets
spec:
  type: secretstores.local.env
  version: v1
scopes:
- demo04-api
